// look at console in browser when open in dev server
// lib.Test.name is not "Test" as it should be
// lib.Test.prototype.constructor.name is not "Test" as it should be
export class Test {
    constructor() {
        this.test = 1;
    }
}

